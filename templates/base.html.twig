<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Sofien{% block title %}{% endblock %}</title>

        <!-- Tailwind CSS CDN (développement rapide) -->
        <script src="https://cdn.tailwindcss.com"></script>

        <!-- Meta pour les moteurs de recherche -->
        <meta name="description" content="Portfolio de développeur web spécialisé Symfony et JavaScript.">

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body class="bg-gray-100 text-gray-800 font-sans leading-relaxed">

        <!-- Header / Navbar -->
        <header class="bg-white shadow-sm">
            <div class="container mx-auto px-4 py-4 flex justify-between items-center">
                <h1 class="text-2xl font-bold">CHERCHARI Sofien</h1>
                <nav class="space-x-4">
                    <a href="{{ path('app_home') }}" class="text-gray-700 hover:text-blue-600">Accueil</a>
                    <a href="{{ path('app_portfolio') }}" class="text-gray-700 hover:text-blue-600">Portfolio</a>
                </nav>
            </div>
        </header>

        <!-- Contenu principal -->
        <main class="container mx-auto px-4 py-8">
            {% block body %}{% endblock %}
        </main>

    <!-- Footer -->
        <footer class="bg-gray-200 w-full py-6 mt-12">
            <h2 class="text-2xl font-semibold mb-4 ml-10">Contact</h2>
            <p class="text-gray-700 ml-16">
                <a href="mailto:88000sofien@gmail.com" class="text-blue-600 hover:underline">
                    88000sofien@gmail.com
                </a>
            </p>
            <p class="text-gray-700 ml-16">
                +33 6 02 12 05 53
            </p>
            <p class="text-sm text-gray-600 text-center">&copy; {{ "now"|date("Y") }} Mon Portfolio - Tous droits réservés</p>
        </footer>
    </body>
</html>
